# 设置中心视觉统一

常用设置、邮箱账号、数据与维护、使用帮助统一使用以下设计规范：

- 内容区最大宽度 1120px，避免不同标签切换时左右跳动。
- 页面介绍区使用相同的标题、说明和右侧状态结构。
- 标签栏使用安静的浅灰绿色底面与白色选中态。
- 功能卡片统一为白色实体表面、14px 圆角、细边框和轻阴影。
- 数据维护在宽屏下双栏展示常用操作，管理员配置保持折叠。
- 使用帮助在宽屏为三栏、平板为两栏、窄屏为单栏。

本次只统一信息层级和视觉表现，不改变原有设置、诊断、备份及帮助功能。
