# 办公助手面板 UI 规范

- 顶部操作统一使用 16px 线性 SVG 图标与 32px 点击区域，顺序为新对话、历史会话、显示选项、关闭。
- “历史会话”和窗口显示方式都作为直接图标入口，不使用只有一个选项的多余下拉菜单。
- 快捷问题采用两列完整展示，避免横向滚动造成文字截断。
- 查询范围使用带文字标签的选择器，明确它控制 AI 检索的邮件范围。
- 输入框与发送按钮使用稳定尺寸；发送按钮固定为 36×36px，不受旧样式或容器伸缩影响。
