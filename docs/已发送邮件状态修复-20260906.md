# 已发送邮件状态修复

版本：1.20260906.1104

- 已读与未读仅用于收到的邮件，用户发出的邮件不再显示未读圆点或未读粗体。
- 本地发送记录统一为已发送展示状态。
- 从 IMAP 服务端同步的已发送邮件即使缺少 `Seen` 标记，也不会被误画成未读。
- 打开已发送邮件后只显示选中态，不触发无意义的服务端已读写入。
- 收件箱未读邮件的圆点、字重以及打开后标为已读的行为保持不变。

验证：49 项离线检查通过；使用隔离邮箱分别核对已发送邮件打开前后、详情展示与收件箱未读邮件。未连接真实邮箱，未发送真实邮件。

安装包未做 Apple 开发者签名和公证。
