# 往来邮件

邮件阅读页的“往来邮件”用于查看当前邮箱中与对方联系人的历史收发记录。

- 自动从当前邮件识别对方邮箱，无需再次搜索。
- 同时展示收到和发出的邮件，按时间倒序排列。
- 当前邮件明确标记，点击其他记录可直接跳转阅读。
- 仅查询当前邮箱已经同步到本机的数据，不跨邮箱账号混合。
- 提供加载、无历史、失败重试和 Esc 关闭状态。

对于自己发出的邮件，系统从收件人中识别对方；对于收到的邮件，则使用发件人作为对方联系人。查询使用完整邮箱地址精确匹配，避免相似地址误归入往来记录。
